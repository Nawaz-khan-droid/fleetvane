/**
 * Database Seeder for FleetVane.
 * Note: When running with the Spring Boot backend architecture,
 * database seeding is handled by backend Flyway/Hibernate data loaders.
 */
export async function seedDatabase() {
  console.log('[FleetVane] Database seeding is managed by the backend service.');
}